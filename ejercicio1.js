//Escribe una función que suma todos los dígitos en un array y 
//otra que multiplique todos los números de otra array.

var numeros = [10,26,15,94];

function suma(){
  console.log(numeros[0]+numeros[1]+numeros[2]+numeros[3]);
} 
suma();

//Multiplicación

var numeros = [10,26,15,94];

function suma(){
  console.log(numeros[0]*numeros[1]*numeros[2]*numeros[3]);
} 
suma();